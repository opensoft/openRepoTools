## Why

`/ctx` currently kills its own pane after writing a handoff and treats `tmux respawn-pane` accepting a command as proof that a fresh Claude session started. A measured failure left the lane `PAUSED` with a valid handoff but no replacement process or visible error, and the planned automatic 65% rollover would reproduce that unsafe boundary without a stronger restart primitive.

**The measured failure is `opensoft/openRepoTools#94`** (Eagle, 2026-09-15T20:59Z, lane `openRepoTools-3`), and the cause was established before this change was implemented rather than inferred from it. `/ctx` respawned its own pane with `LANE_START_FRESH=1 … lane openRepoTools-3` and what came up was `claude --name openRepoTools-3 --resume <the uuid the /ctx had just paused>`. `lane-start` was not at fault — both of its `(( ! fresh ))` gates are present in the installed copy and in PR #87's branch version — and neither was `lane`, whose available branch ends in a plain `exec`. The variable never arrived: the installed launcher re-creates its child through tmux, and `claude-profile`'s own `act1_compose_child_command` states the rule it has to obey — *"`tmux new-session` hands the command the SERVER's environment, not this client's, so a variable the operator exported in their own shell reaches the child only if it is written into the command string"* — which is why it threads six values explicitly, and `LANE_START_FRESH` is not one of them. The defect capture's window, `claude-max-001-20260915205926-456327:@6`, is that launcher's own `claude-<profile>-<timestamp>-<pid>` pattern, so this is what happened rather than what might have.

Two consequences follow, and they are what this change is:

- **An environment seam cannot carry the fresh-session requirement**, because the boundary it must cross belongs to another repository; a seventh threaded variable would move the same fragility one release along. The requirement becomes a durable restart intent on disk, written before the old process is killed and read back whatever the environment did.
- **Nothing noticed**, because what tmux started *was* the launch: a launch that ends takes the pane with it. Amendment 11's own invariant is *"No path the launcher opened exits the pane"*, and `/ctx` opened a path that did.

## What Changes

- Make context restart a generation-fenced transaction that preserves exact fresh-session launch intent before replacing the old process.
- Replace direct respawn into the human-facing `lane` dispatcher with a pane-resident supervisor and a narrow internal launch operation.
- Keep a diagnostic and retry surface alive when profile resolution, launcher execution, Claude startup, or readiness confirmation fails.
- Keep the lane non-running until the replacement's transcript, agent, directory, binding, and liveness are positively confirmed.
- Require lifecycle ownership and mandatory preservation writes to succeed before `/ctx` can kill the current process.
- Separate model-authored semantic handoff preparation from deterministic state, tmux, launch, and retry mechanics.
- Add real process-lifecycle tests that distinguish tmux command acceptance from a confirmed replacement session.

## Capabilities

### New Capabilities

- `supervised-context-restart`: Defines the guarded `/ctx` transaction, durable restart intent, surviving pane supervisor, internal fresh-session launch, readiness confirmation, and idempotent failure recovery.

### Modified Capabilities

None. This repository currently has no baseline OpenSpec capabilities; relationships to the in-progress crash-recovery and automatic-rollover changes are expressed as dependencies rather than fictional baseline modifications.

## Dependencies and cross-links

- **`opensoft/openRepoTools#94`** — the measured failure this change answers. Its root cause is quoted above and recorded as a comment on the issue.
- **`opensoft/openRepoTools#91`** (`add-crash-consistent-lane-worktree-recovery`) — the lane control root, the `RUNNING → SWAPPING → SWAPPED → CLOSED` lifecycle and the generation/operation fence the restart intent is meant to sit beside. That change is in flight as **pull request `opensoft/openRepoTools#97`** on `feat/crash-consistent-lane-worktree-recovery` and is **not on `main`**, so the restart intent is implemented as a sibling sidecar under the *same* control root, resolved by a `lane_control_root` written to be byte-identical to #91's, and the `SWAPPED → RUNNING` finalization is made through #91's `set-lane-state` **where the workstation has it** and skipped in silence where it does not. Nothing here blocks on #91 landing, and nothing here re-implements it.
- **`add-automatic-context-rollover`** — consumes this change's readiness-confirmed completion rather than raw `respawn-pane` acceptance.
- **Lane collision protocol** — Amendment 17(f) (the record before the kill), Amendment 17 Addendum 1 (h)–(k) (the kind, the writer count, one worktree one writer), Amendment 18(h) (one binding), Amendment 18 Addendum 2 (i-8) (the respawn's two doors, and *"no word is kept on `PATH` for it alone"*), Amendment 8(f) (ending somebody's process is not a boundary script's act), Amendment 8(a) step 4 / `R-A11-5` (a swap is never left unwritten) and `R-A8-1` (the `SessionStart` hook never writes, never touches the network, always exits 0).

## Review verdicts — the implementation against the documents it came from

Brett Heap's brainstorm packet (`ideation/brainstorm/supervised-context-restart-*.md`, captured 2026-09-15) is committed with this change, as `opensoft/openRepoTools#97` committed its own. Each document was read against what shipped, and where the two differ the difference is a numbered decision in `design.md` rather than a silence.

| document | verdict |
|---|---|
| `ideation/README.md` | **matches**, carried with the one bullet this branch's files answer to. Brett's working copy lists three packets; the other two are committed by their own changes (#97 carries the crash-recovery packet), and a link to a document no branch has is worse than a list that grows one packet at a time. The merge with #97's copy is a union of the bullets. |
| `…-overview.md` | **matches.** Every goal and every non-goal is honoured, and two of the non-goals are honoured by refusal rather than by omission: *"Making the human-facing `lane` dispatcher a narrower internal API"* — `lane` is not touched at all, the restart simply stops calling it (decision 11); and *"Adding a continuously polling system daemon"* — the supervisor exists for ONE operation and ends with its child. Its system model's `STARTING` box is answered as the alternative the document itself offers: no fifth lifecycle word, a fenced attempt beneath `SWAPPED` (decision 3). Its open question *"whether the supervisor belongs in openRepoTools or workBenches"* is answered openRepoTools, and narrowed by decision 10 to not being a new word anywhere. |
| `…-durable-intent.md` | **matches, with its field spellings changed and six fields added.** Its YAML sample writes `operation_id`, `directory` and `requested_at`; the record writes `operation`, `dir` and `created`, because it shares its grammar, its reader, its atomic replace and its three-rung control root with #91's `lane-state.yaml` byte for byte (decision 14), and a second spelling of one estate's fields is a second scheme. Six fields are added, each because a requirement of this change cannot be checked without it: `digest` (*a changed handoff blocks launch*), `pane` and `window` (the supervisor must prove it stands in the pane the `/ctx` respawned), `new_transcript` (readiness has nothing to confirm against otherwise), `attempt`, and `workstation`. Its open question *"which failure details are safe to persist without capturing credentials"* is answered by construction — there is no credential field and the profile is a manifest key, asserted by a test. Its *"can an operator cancel a pending restart"* is **not** answered: task 6.5, open. |
| `…-pane-supervisor.md` | **deviates, recorded as decision 10.** It proposes a new command, `lane-restart-supervisor --lane <lane> --operation <id>`. Amendment 18 Addendum 2 (i-8) took `restart` off `PATH` on the ground that *"no word is kept on `PATH` for it alone"*, so the supervisor is `lane-handoff --supervise --lane <lane> --operation <id>` — everything the document requires (a separate process, the thing tmux starts, resolved by absolute path, the launcher as its child, never `lane`, never a picker) and nothing added to a person's `PATH`. Its *"a cancellation or inspection command where supported"* is half-delivered: retry, read and by-hand are printed; cancel is task 6.5. Its open question *"remain for the full Claude lifetime or only through confirmed startup"* is answered — the full lifetime, and a child that exits AFTER readiness is reported and never relaunched. |
| `…-semantic-mechanical-boundary.md` | **partly delivered, and the missing half is named.** Its phase 2 lands whole: `/ctx`'s skill step is one command and the executable `tmux respawn-pane` recipe is gone from it, which is the document's own *"allows a session to reconstruct the destructive tail instead of calling one authoritative transaction"*. Its phase 1 **structured staged payload does not** — tasks 3.1–3.3, open, listed as deferred in `tasks.md` §0. The model's semantic work still reaches the backend as the handoff file it wrote and the writers it polled, not as an operation-scoped payload with binding checks. |
| `…-startup-confirmation.md` | **matches, with the evidence narrowed to what `main` can actually prove** (decision 15). It asks for nine facts; the predicate asks four — the child alive, exactly one live holder of the lane, that holder's transcript the one `lane-start` recorded it was preparing (or at minimum NOT the one the `/ctx` paused), and the pane. Agent, profile and directory are fenced *earlier*, where a conflict refuses the launch before it is made, rather than re-checked after. Its *"Only confirmation may write `RUNNING`"* is scoped by decision 12 — it is #91's lifecycle snapshot that waits, never Amendment 8(d)'s append-only `RESUMED` line, whose removal would break three other readers — and the residual that follows on a `main` with no snapshot is stated there rather than hidden. Its *"how long may `STARTING` remain"* is answered: 60 seconds, then INDETERMINATE, which kills nothing. |
| `…-synthesis-reliable-rollover.md` | **matches**, with one collision resolved: its *"Failures before `SWAPPED` preserve the old session wherever it is still alive"* meets #91's landed *"THE LIFECYCLE NEVER REFUSES THE SWAP"*. Decision 13 draws the line Amendment 17(f) already draws — the RECORD is never refused, the KILL always is when the record is incomplete — so both are kept and neither is overruled. |
| `proposal.md` | **matches**, extended with the measured cause and a dependencies/cross-links section. No line of the original was removed. |
| `design.md` | decisions 1–9 stand as written; **decisions 10–15 are added**, one per deviation. Two of the original decisions could not be taken as written — decision 5's new installed command, and decision 7's unscoped *"stop writing `LIVE`/`RUNNING` before `exec`"* — and each is answered in place, beside the text it qualifies, rather than quietly. |
| `specs/supervised-context-restart/spec.md` | four requirements **extended** (the intent as the authority, with #94's launcher boundary as the reason; the supervisor as a mode and not a `PATH` word; the pane invariant and the never-kill rule; the first prompt holding *with no fresh-session environment variable present at all*), and three scenarios added — one of them #94's own measured case. No requirement and no scenario was removed. |
| `tasks.md` | 29 of 46 ticked. §0 is added, naming what this slice landed and what it did not with a reason for each, and three tasks are **rewritten rather than ticked as written** (4.1, 4.6 and 5.1) and say so in their own lines. |

**The one deviation from in-force protocol text**, and it is not treated as ratified by this change: Amendment 18 Addendum 2 (i-8) names two doors for the `/ctx` respawn — `lane <name>`, *"or through the launcher directly"* — and this change respawns into neither, because what tmux now starts is the supervisor. The supervisor then takes the clause's SECOND door. The clause's stated ground (*no word on `PATH` for it alone*) is honoured exactly; its stated mechanism is not. Task 7.4 owes the amendment, and the divergence is named in `design.md` decisions 10 and 11, in `lane-handoff`'s own comment at the respawn, and in this change's pull request.

## Impact

The change affects `commands/ctx.md`, the handoff skill, `lane-handoff`, `lane-start`, `lanes-edit.sh`, lane lifecycle sidecars, tmux process management, and lane helper tests. It introduces an integration boundary with the installed `pclaude` launcher owned by workBenches: either the new supervisor invokes that launcher as a child, or coordinated work there supplies the equivalent non-`exec` supervision seam. The `add-crash-consistent-lane-worktree-recovery` change must expose fail-closed transition ownership, and `add-automatic-context-rollover` must depend on confirmed supervised restart rather than raw respawn acceptance.
